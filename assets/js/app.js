'use strict';

/**
 * Define the app module ("App")
 *
 * Bring any module dependencies (ngRoute, etc.)
 */

(function() {
    var dependencies = [
        'ngResource',
        'ngRoute'
    ];

    angular.module('app', dependencies);
})();
